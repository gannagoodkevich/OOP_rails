name: Rubocop

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Rubocop Linter
        uses: actions/checkout@v1
        with:
          repository: gannagoodkevich/OOP_rails
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}